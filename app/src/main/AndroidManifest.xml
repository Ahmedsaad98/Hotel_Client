<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ahmedalraziki.graduation_client">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Graduation_client">
        <activity android:name=".Activites.Payments"></activity>
        <activity android:name=".Activites.Pricing" />
        <activity android:name=".Activites.My_Res" />
        <activity android:name=".Activites.Dashboard" />
        <activity android:name=".Activites.Sign_In" />
        <activity android:name=".Activites.Make_Res" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>